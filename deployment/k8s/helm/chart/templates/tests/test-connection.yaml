apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-webapp-example.fullname" . }}-test-connection"
  labels:
    {{- include "go-webapp-example.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "go-webapp-example.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
