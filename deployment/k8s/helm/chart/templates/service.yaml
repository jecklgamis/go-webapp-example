apiVersion: v1
kind: Service
metadata:
  name: {{ include "go-webapp-example.fullname" . }}
  labels:
    {{- include "go-webapp-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "go-webapp-example.selectorLabels" . | nindent 4 }}
